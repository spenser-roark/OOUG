<% if (!@ownership.empty?) %>
<div class="sidebar pull-right visible-desktop">
  <div class="sideConsole">

<div class="content .navbar-inverse">
<ul>

<li><%= link_to "All Games",  inventory_games_path(current_user) %></li>

  <% @gameConsoles.each do |game| %>

<li><%= link_to game, inventory_game_path(current_user, game) %> </li>

<% end %>

</ul>
</div>  
</div>
</div>

<div class="hidden-desktop" style="text-align: center">
Filter by console <select id="game-list">
	<option value="all">All Games</option>
  <% @gameConsoles.each do |game| %>
	<option value="<%=game%>"><%=game%></option>
<% end %>

</select>
</div>

<% end %>
